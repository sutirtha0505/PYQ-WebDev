# HTML & CSS Practice Project

This repository contains a collection of beginner-friendly HTML and CSS exercises from the 2024 question papers. Each question folder is a standalone webpage task that focuses on core web design concepts such as layout, styling, responsiveness, navigation, and section-based content design.

## Project Structure

```text
PYQ/
├── README.md
└── 2024/
    ├── set-1/
    │   ├── Q1/
    │   ├── Q2/
    │   ├── Q3/
    │   ├── Q4/
    │   ├── Q5/
    │   ├── Q6/
    │   ├── Q7/
    │   └── Q8/
    ├── set-2/
    │   ├── Q1/
    │   ├── Q2/
    │   ├── Q3/
    │   ├── Q4/
    │   ├── Q5/
    │   └── Q6/
    └── set-3/
        ├── Q1/
        ├── Q2/
        ├── Q3/
        ├── Q4/
        ├── Q5/
        ├── Q6/
        ├── Q7/
        └── Q8/
```

Each question folder contains an `index.html` file and, where required, related CSS and supporting HTML files for that specific exercise.

## What is being practiced?

These exercises cover:

- HTML page structure
- CSS styling and selectors
- Flexbox and layout design
- Section navigation using anchor links
- Basic page responsiveness
- Styling for personal/profile pages and content blocks

## How to View the Project

You can open any HTML file directly in a browser, or run a local web server from the project root.

### Option 1: Open directly

Open any `index.html` file in your browser.

### Option 2: Run a local server

From the project root:

```bash
cd "/Users/sutirtha_05/Desktop/1st year/PYQ"
python3 -m http.server 8000
```

Then open:

```text
http://localhost:8000/2024/set-1/Q1/
```

Replace `set-1/Q1` with any available set and question folder you want to preview, such as `2024/set-3/Q8`.

## Notes

- This project is intended for learning and practice.
- Each question is independent and can be opened individually.
- The design and HTML structure are intentionally simple and beginner-focused.

## Author

This project is a web development learning exercise set created for HTML/CSS practice.
